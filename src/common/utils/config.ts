export const API_CONFIG = {
  BASE_URL: "http://localhost:3000/api",
  ENDPOINTS: {
    USERS: "/users",
    AUTH: "/auth",
    MENUS: "/menus",
    CATEGORIES: "/categories",
    ITEMS: "/items",
    IMAGES: "/images",
  },
} as const;